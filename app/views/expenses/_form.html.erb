<%= simple_form_for [@group, @expense] do |f| %>
  <%= f.input :date , as: :date%>
  <%= f.input :tag_id do %>
	  <%= f.select :tag_id, @group.tags.map { |r| [r.name, r.id] } %>
  <% end %>  
  <%= f.input :name %>
  <%= f.input :description %>    
  <%= f.input :cost %>  
  <%= f.button :submit %>
<% end %>

